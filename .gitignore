node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.local

# Bundler output — regenerate with: node generate-loader.js <dir>
loader.html

# Decryption keys. Never commit these: anyone with the key can read the
# bundle, and the key is not recoverable if lost.
*.key
key.txt
mykey.txt
